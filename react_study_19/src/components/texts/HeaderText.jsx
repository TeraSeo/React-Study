import { Typography } from "@mui/material";

function HeaderText() {
    return (
        <Typography component="h1" variant="h5">
            Sign in
        </Typography>
    );
}

export default HeaderText;